# Simple Site Builder

Профессиональный CMS-редактор для создания страниц с помощью drag & drop.

## Возможности

- 🎨 **Drag & Drop редактор** - интуитивный редактор на основе блоков
- 📝 **Множество типов блоков** - заголовки, текст, изображения, кнопки
- 🎯 **Панель свойств** - редактирование всех параметров блоков
- 👁️ **Предпросмотр в реальном времени** - видите результат сразу
- 💾 **Автосохранение** - изменения сохраняются автоматически
- 📤 **Экспорт кода** - генерация HTML и React компонентов
- 📱 **Адаптивный дизайн** - работает на всех устройствах

## Технологии

- **Vite** - быстрая сборка
- **TypeScript** - типобезопасность
- **React** - современный UI
- **shadcn/ui** - компоненты интерфейса
- **Tailwind CSS** - стилизация

## Установка и запуск

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev

# Сборка для production
npm run build

# Предпросмотр production сборки
npm run preview
```

## Использование

1. Создайте новую страницу через кнопку "New Page"
2. Добавляйте блоки из палитры слева
3. Редактируйте блоки через панель свойств справа
4. Используйте drag & drop для изменения порядка блоков
5. Экспортируйте готовый код в формате HTML или React

## Структура проекта

```
src/
  ├── components/     # React компоненты
  │   ├── blocks/    # Компоненты блоков
  │   └── editor/    # Компоненты редактора
  ├── contexts/      # React контексты
  ├── lib/           # Утилиты и генераторы кода
  ├── pages/         # Страницы приложения
  └── types/         # TypeScript типы
```

## Лицензия

MIT
